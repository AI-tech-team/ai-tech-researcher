# 読み取り枠が戻ったらやること

2026-09-14〜15、Turso の読み取り枠切れ（`LibsqlError: BLOCKED`）で本番DBに一切読めない間に、
バックアップ（`backups/backup_2026-09-13.json`）と本番HTTPだけで進めた分の**答え合わせ待ち**をまとめる。
上から順に、**DBが戻った直後にやる**。

## 0. 前提（オーナーしかできない）

- [ ] Turso の使用量を確認し、枠を戻すかプランを上げる（org: `meguru-v1`）。
      こちらからは使用量画面もプラン変更も触れない。`.env.prod.readonly` にあるのは
      `TURSO_AUTH_TOKEN` と `TURSO_DATABASE_URL` だけで、Platform API のトークンは無い。
- [ ] 復旧確認: `npx tsx scripts/_probe_db.ts` が `READ OK`、`https://cernoval.com/api/health` が 200。

## 1. すぐ見る（本番ログ・1回のパイプラインで出る）

- [ ] `[Report] なぜ重要か` と `[Report] 何が起きたか（対照）` の2行。
      **基準線: 直近21号 24.8% / 対照 94.1%**（直近3号なら0%）。案Bの効果はここで判定する。
      1日の値では結論を出さない。
- [ ] `[KeyPoints] 未処理の残りn件（1回の上限m件）` — 上限を上げるべきかを**数字を見てから**決める。
- [ ] `[Translate] 走査n件中m件が要翻訳` — 同上。
      ⚠ ここは一度 `COUNT(title_ja IS NULL)` で 10,674件と誤って数えた（真値160件・67倍）。
      いまは走査窓ベース。数字が跳ねたらまず数え方を疑う。
- [ ] `[Log] パイプライン記録: … （直近N回の中央値: …）` が出ているか（判断ログ㉕）。
- [ ] `[FeedHealth] 降格中 n本` が出ているか。`⚠ 降格しない:` が出たらそれは**こちら側の不具合**の印（㉖）。
- [ ] `[Evolve] ⚠ activeなkeywordソースが0件` が出ているか（㉗）。

## 2. 公開面の実機確認（Playwright スクショ必須）

- [ ] **記事のタグ**（㉝）。`#hn-score:770` `#techcrunch.com` `#arxiv` が消え、話題タグだけが出ているか。
      枠がメタデータで埋まっていた問題なので、**タグ行が増えている**はず。
- [ ] **関連トピック**（㉜）。チップが減り、**押して404になるものが無い**こと。
      修正前は938種中50種が404、658種が薄いページ行きだった。
- [ ] **ベンチマーク**（㉚）。`事例 150%` `平均所得税率 14.1%` `MMBench2 427hours` が消えていること。
- [ ] **判明している事実**（㉛）。`コーディング能力: 向上` のような中身ゼロの行が消えていること。

## 3. 数日〜数週かけて見る

- [ ] **焼き直し**（㉔）。同じ話が連続する号に出ていないか。修正前は
      ナビエ・ストークス論争が 09-09 → 09-12 → 09-13 と3号に出ていた。n=1では判断しない。
- [ ] **読了時間**。3分以内が続くか。09-13(2:36) と 09-14(2:28) の**2号だけ**では「安定」と言えない。
- [ ] **robots拒否の7ソース**が自動で `stopped` になったか（収集側に自動停止を入れてある）。
- [ ] **バッチ抽出**（㉒）。`PIPELINE_MODE=batch_submit` → `batch_fetch` を回すと
      `metadata.articleId` で対応づくか。旧ジョブが無いので初回から新方式で動くはず。

## 4. DBが無いと測れなかったもの

- [ ] **知識抽出の本文3,000字上限**（`buildExtractionPrompt`）。バックアップに `rawContent` は
      含まれない（第三条で書き出さない設計）ので**オフラインでは測れない**。
      DB復旧後に「imp>=7の記事の本文長の分布」と「3,000字より後ろにベンチ名が出る記事の割合」を測る。
      推測で上限を上げない。
- [ ] 同じ理由で、要点生成の `rawContent.slice(0, 2500)` も未測定。

- [x] **障害画面がISRキャッシュに載るのを止められるか** → ✅ 解決（2026-09-15・決定54）
  - 事実: 障害中の `/articles/26178` は `X-Nextjs-Prerender: 1` / `X-Vercel-Cache: HIT` のまま
    `Age` が伸びた（79→82→86・3秒間隔）。`OutageNotice` は404とまったく同じように格納されていた。
    つまり**復旧後も最大1時間、実在する記事に「お見せできません」が出続ける**状態だった。
  - 測り方が見つかった: **本番デプロイは要らなかった**。枠切れ中の本番DBを繋いだまま
    `next build --webpack` → `next start` すれば、ローカルで障害がそのまま再現する
    （`.next/server/app/articles/26178.html` が実際に書き出される＝ファイルの有無で判定できる）。
  - 測った結果、検討していた2案は**どちらも使えなかった**:
    ① `await connection()` → ISRルートの中では呼べず 500。疑っていたとおり、同梱docsの
       「ビルド時プリレンダの除外」以上のことはしない。
    ② `throw` → キャッシュは確かに書かれないが、ISR生成中の例外は `error.tsx` に落ちず、
       **素の "Internal Server Error"（21バイト）**が返る。文面ごと消えるので不採用。
  - 採った手: `src/middleware.ts` の `exists()` を3値（yes/no/unknown）にし、unknown は
    **ISRルートに入る前に** `/outage`（force-dynamic）へ rewrite する。3ルートとも `no-store` の
    34KBの画面が出て、ISRファイルは生成されないことを実測（PC/モバイルのスクショも取得済み）。
  - 残った確認（復旧後）: DBが戻ったときに `exists()` が 'yes' を返す通常経路が無傷か。
    論理上は `return true` → `return 'yes'` の置換のみだが、DBが落ちている間は実機で踏めない。
    `/articles/26178` が記事本体を返し、`X-Nextjs-Prerender: 1` が復活すれば正常。
    （`/topic` にあった `revalidate` は `generateStaticParams` が無く無効だったため撤去済み）

- [ ] **RSSの503が正しく出るか**（㊱で入れたガード）
  - ✅ 障害側は検証済み（2026-09-15 本番実測）: `/feed.xml` は
    **HTTP 503 / `Cache-Control: no-store` / 28バイト**。嘘の空フィードは配られていない。
  - 残りは復旧側だけ:
  - `curl -sD - https://cernoval.com/feed.xml -o /dev/null` で **200 かつ `<item>` が入っている**こと。
  - 復旧直後は `stale-while-revalidate=86400` のせいで**障害中の空フィードが最大24時間残る**。
    `/api/revalidate` では消せない（route handlerのCDNキャッシュ）ので、
    復旧を確認したら**空でないことを目視するまで数回叩く**（s-maxage=1800で最大30分）。

- [ ] **URLのエンティティ混入をDB側でも揃える**（㊲・2026-09-15）
  - 読者への実害は `safeHttpUrl` のデコードで既に消えている。これはDBの値を揃えるだけ。
  - `npx tsx scripts/fix_entity_urls.ts` でまずドライラン（何件変わるか・衝突が何件か）。
  - 数字がバックアップ実測の **2,083件** と大きくずれていたら、そのまま `--apply` せず原因を見る。
  - 本番DB変更なのでオーナーのGOを得てから `--apply`。
  - 衝突（解いた結果が既存行と重複）が出たら、それは「同じ記事が2行」ある状態。統合はオーナー判断。

- [x] **サイト側の箇条書き判定を `markdown-lines.ts` に寄せる**（㊵）→ ✅ **既に完了していた**
  - 2026-09-15 に確認: `src/components/Markdown.tsx` は既に `BULLET_LINE`（119行）と
    `bulletContent`（124行）を使っている。**同じ日に直したのにこの項目を消し忘れていた**だけ。
  - ⚠ 教訓: 繰り越し表が古いと、同じ調査を二度やる（実際にこの項目で一度やり直した）。
    直した項目はその場で消すか ✅ にする。

- [ ] **構造化データの `image` が実機で1200×630を指しているか**（㊷・2026-09-15）
  - `curl -s https://cernoval.com/articles/<実在id> | grep -A2 '"image"'` で
    `/articles/<id>/opengraph-image` と `width: 1200` が出ること。レポート側も同様。
  - Google Search Console のリッチリザルトテストに1本かけて、エラーが無いことを見る。
  - 障害中は記事ページが障害画面になるため確認できない（この項目はそのため残している）。

- [ ] **公開に戻した直後に障害が起きると、sitemap が「もうこの13件しかない」と申告する**（2026-09-15 発見・未修正）
  - いまは無害: `SITE_NOINDEX = true` のあいだ sitemap は **DBを1つも引かず**固定13URLだけ返す
    （2026-09-14 の止血・意図どおり）。本番実測でも 200 / 13URL で、これは正しい姿。
  - 問題は `SITE_NOINDEX = false`（公開）にした後。`sitemap.ts` の3つの取得は
    `try { … } catch { /* 入口ページのみ */ }` で囲まれているが、**3つの取得関数はどれも内部で
    catch して `[]` を返す fail-open なので、この catch は一度も走らない**。
    実際に起きるのは「例外」ではなく「空で返る」で、そのまま固定13URLの sitemap が
    **`lastmod = 今` つきで 200** で出る＝検索エンジンに「もうこの13件しかありません」と
    新鮮な判子つきで申告することになる。欠落ではなく積極的な嘘で、約22,000URLが落とされうる。
  - RSS では既に同じ判断をして 503 を返している（㊱）。**同型の枝が1本だけ直っている状態**。
  - ⚠ **素直な修正（throw）は試して却下した**（2026-09-15 実測）: `sitemap.ts` は配列を返す規約で
    ステータスを選べないので throw するしかないが、sitemap はビルド時にプリレンダされるため
    **`next build` が終了コード1で落ちる**（ローカルで `SITE_NOINDEX=false` + 枠切れDBで実測）。
    障害中こそ修正をデプロイしたいのに、デプロイ自体ができなくなる＝代償が大きすぎる。
  - **提案（GOがあれば実装）**: `app/sitemap.ts` を **`app/sitemap.xml/route.ts`（route handler）に
    移す**。feed.xml と同じ形になり、0件のときに `503 + Retry-After + no-store` を返せる。
    ビルドは落ちない（feed.xml は現に503を返しながらビルドも本番も通っている）。
    代償: `MetadataRoute.Sitemap` の型と `generateSitemaps()` を手放し、XMLを手で組む（約50行）。
  - 急がない理由: 公開前は発火しない。**ただし公開は `SITE_NOINDEX` を false にするだけ**なので、
    そのとき誰も sitemap を見直さない。公開作業とセットで判断すること。

## 5. オーナー判断（こちらでは触らない）

- [ ] `alerts`(1,722行) — 参照0。07-07の私用機能撤去の取り残し。消すか残すか。
- [ ] `research_questions`(326行) — INSERTのみで消費側は撤去済み。消すか、リサーチ機能の受け皿として残すか。
- [ ] **キーワード発見**（㉗） — 修理（候補を試験的に巡回させる）か撤去（発見ごと止める）か。
      現状は毎日 LLM を1回呼んで、14日後に必ず捨てられる行を作っている。
- [ ] **降格中のRSS 14本**（㉖） — 5本は今も記事を出している（`pc.watch` `trullion` `openlm.ai` ほか）。
      戻すかは中身を見る判断。機械的に戻すと流入だけ増えて処理の上限を食う。
- [ ] **Reddit記事 2,306件** — 09-10 の robots ゲート導入より前に集めた分が今も公開面に見えている。
      `DIGEST_EXCLUDED_HOSTS` はダイジェストから外すだけ。削除は本番への破壊操作。
- [ ] `ENTITY_ALIAS_KEYS` が4件（`gpt-4o` `claude 3.5 sonnet` `gemini 1.5`）で**全部旧世代**。
      現行（GPT-5.6 / Claude Opus 5 / Gemini 3.x）を1件もカバーしていない。
      ただし何を別名とみなすかは中身を見る判断なので機械的には足せない（㉙）。

## 6. 公開するとき

- [ ] `v2/src/lib/site.ts` の `SITE_NOINDEX` を `false` にする。
      いまは全ルートに `X-Robots-Tag: noindex` が付き、sitemap も静的13URLだけ。
      `robots.txt` の `Allow: /` は**正しい**（noindex はヘッダなので取得できないと読めない）。㉓

- [ ] **事業者情報の env を入れる（法令の穴。決めるのはオーナー）**
  - いま朝刊メールのフッタは**2行しか無い**（2026-09-15 に実物を描画して確認）:
    「配信を停止する（1クリックで停止します）」「設定の変更は Cernoval のプロフィールから。」
  - `NEXT_PUBLIC_OPERATOR_NAME` / `_ADDRESS` / `NEXT_PUBLIC_CONTACT_EMAIL` が未設定なので、
    `mailFooter()` は送信者名・所在地・問い合わせ先の行を**静かに省いている**（配線は済んでいる）。
  - 効く場所: メールフッタ（特定電子メール法4条）／`/privacy` の保有個人データの節
    （個人情報保護法32条・decisions.md に既述）／`/terms`／`/feedback`／ナビ。
  - ⚠ **単なる env 入力ではない。**[[privacy-identity-scrub]] で身元を徹底的に匿名化した経緯と
    正面からぶつかる（氏名・住所を受信箱に出すことになる）。
    逃げ道は32条側にはある（「本人の求めに応じて遅滞なく回答」）が、
    特定電子メール法4条は**広告宣伝メールに該当する場合**に表示を求める。
    無料・広告なしの現状で該当するかを含め、**有料配信に切り替える前に決める**。
  - いま急がない理由: 受信者はオーナー本人のアカウントのみ（登録4人・配信ON3人）。

- [ ] **朝刊の紙面スクショ**（㊼・2026-09-15に撮れなかった）
  - `cleanText` でコード記法とリンク記法を落としたが、`DigestBody` はCSSモジュールを含むため
    単体描画できず、本番はDB障害で朝刊が出ないので実機スクショが撮れていない。
  - 復旧後に `/` と `/reports/<コードを含む号>`（例 id=301・2026-09-04）を撮り、
    バッククォートが消えて文が自然に読めることを目で確認する。

